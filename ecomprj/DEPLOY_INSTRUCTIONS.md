# Инструкция по деплою на Render.com

## ✅ Что уже сделано в проекте:

1. ✅ Настроен **WhiteNoise** для статических файлов
2. ✅ Настроено подключение к PostgreSQL через переменные окружения
3. ✅ DEBUG и SECRET_KEY берутся из переменных окружения
4. ✅ Создан **build.sh** скрипт для автоматической сборки
5. ✅ Добавлен **gunicorn** в requirements.txt
6. ✅ Настроен Procfile для запуска

## 📋 Шаги для деплоя на Render.com:

### 1. Подготовка репозитория Git

Убедитесь что ваш проект находится в Git репозитории (GitHub, GitLab или Bitbucket):

```bash
git init
git add .
git commit -m "Prepare for Render deployment"
git branch -M main
git remote add origin YOUR_REPO_URL
git push -u origin main
```

### 2. Создание PostgreSQL базы данных на Render

1. Зайдите на [render.com](https://render.com) и войдите/зарегистрируйтесь
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `ecomprj-db` (или любое имя)
   - **Database**: `ecomprj_db`
   - **User**: (оставьте по умолчанию)
   - **Region**: выберите ближайший регион
   - **Plan**: **Free** (бесплатный тариф)
4. Нажмите **"Create Database"**
5. **ВАЖНО**: Скопируйте **Internal Database URL** (понадобится для следующего шага)

### 3. Создание Web Service на Render

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш Git репозиторий
3. Заполните настройки:

   **Basic Settings:**
   - **Name**: `multivendor-shop` (или любое имя)
   - **Region**: тот же что и для БД
   - **Branch**: `main`
   - **Root Directory**: (оставьте пустым)
   - **Runtime**: **Python 3**
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn ecomprj.wsgi:application`

   **Advanced Settings (нажмите "Advanced"):**

4. Добавьте переменные окружения (Environment Variables):

   ```
   PYTHON_VERSION = 3.11.0
   
   SECRET_KEY = (сгенерируйте новый: https://djecrety.ir/)
   
   DEBUG = False
   
   ALLOWED_HOSTS = your-app-name.onrender.com
   
   CSRF_TRUSTED_ORIGINS = https://your-app-name.onrender.com
   
   DATABASE_URL = (вставьте Internal Database URL из шага 2)
   
   STRIPE_SECRET_KEY = your-stripe-secret-key
   STRIPE_PUBLIC_KEY = your-stripe-public-key
   ```

   **ВАЖНО**: Замените `your-app-name` на реальное имя вашего сервиса!

5. **Plan**: выберите **Free** (бесплатный)
6. Нажмите **"Create Web Service"**

### 4. Ожидание деплоя

Render автоматически:
- Установит зависимости из requirements.txt
- Выполнит `collectstatic` для сбора статических файлов
- Применит миграции базы данных
- Запустит приложение

Это займет 5-10 минут. Следите за логами в реальном времени.

### 5. Создание суперпользователя

После успешного деплоя, создайте админа:

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **"Shell"** (в верхнем меню)
3. Выполните команду:
   ```bash
   python manage.py createsuperuser
   ```
4. Следуйте инструкциям для создания пользователя

### 6. Проверка сайта

Откройте ваш сайт: `https://your-app-name.onrender.com`

Если видите только HTML без стилей - подождите 2-3 минуты, иногда статика прогружается с задержкой при первом запуске.

## 🔧 Решение проблем:

### Проблема: Сайт показывает только голый HTML (без CSS/JS)

**Решение:**
1. Проверьте что WhiteNoise в MIDDLEWARE (уже настроено)
2. Убедитесь что build.sh выполнился без ошибок (смотрите логи)
3. Проверьте что `STATICFILES_STORAGE` настроен (уже настроено)
4. Подождите 2-3 минуты после первого деплоя
5. Очистите кеш браузера (Ctrl+Shift+R)

### Проблема: Ошибки валидации при регистрации

**Возможные причины:**
1. **Проблемы с миграциями:**
   ```bash
   # В Shell на Render:
   python manage.py migrate --run-syncdb
   ```

2. **Проблемы с полями User модели:**
   - Проверьте что все поля в userauths.models.User соответствуют форме регистрации
   - Убедитесь что required поля заполняются

3. **Проблема с email backend:**
   - Для тестирования установите в settings.py:
     ```python
     EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
     ```

### Проблема: 500 Internal Server Error

**Решение:**
1. Откройте логи в Render Dashboard
2. Найдите точную ошибку
3. Часто это проблема с миграциями - выполните:
   ```bash
   python manage.py migrate
   ```

### Проблема: Static files 404

**Решение:**
1. Проверьте что в settings.py:
   ```python
   STATIC_ROOT = os.path.join(BASE_DIR, "staticfiles")
   ```
2. В Shell выполните:
   ```bash
   python manage.py collectstatic --no-input
   ```

## 📝 Важные замечания:

1. **Бесплатный план Render:**
   - Сервис "засыпает" после 15 минут неактивности
   - Первый запрос после "сна" может занять 30-60 секунд
   - База данных будет удалена через 90 дней (сделайте бэкап!)

2. **Медиа файлы:**
   - На бесплатном плане Render НЕ сохраняет загруженные файлы при перезапуске
   - Рекомендуется использовать AWS S3 или Cloudinary для media файлов
   - Настройки S3 уже есть в вашем проекте (boto3, django-storages)

3. **База данных:**
   - Делайте регулярные бэкапы
   - Можете экспортировать данные командой:
     ```bash
     python manage.py dumpdata > backup.json
     ```

## 🔐 Безопасность:

1. ✅ SECRET_KEY вынесен в переменные окружения
2. ✅ DEBUG=False на production
3. ✅ ALLOWED_HOSTS настроен правильно
4. ⚠️ Не коммитьте .env файл в Git (добавьте в .gitignore)

## 📞 Поддержка:

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте что все переменные окружения установлены правильно
3. Убедитесь что DATABASE_URL указывает на вашу БД на Render

---

**Готово! Ваш multivendor-shop должен работать на Render.com** 🚀
