# 🔄 INTEGRATION & WORKFLOW DOCUMENTATION

## Оглавление
1. [Обзор взаимодействия систем](#обзор-взаимодействия-систем)
2. [Workflow пользовательских сценариев](#workflow-пользовательских-сценариев)
3. [Request-Response циклы](#request-response-циклы)
4. [Data Flow диаграммы](#data-flow-диаграммы)
5. [Deployment Pipeline](#deployment-pipeline)
6. [Monitoring & Logging](#monitoring--logging)

---

## Обзор взаимодействия систем

### Full Stack Integration Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT TIER                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Web Browser (Chrome, Firefox, Safari, Edge)                        │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │   │
│  │  │   HTML   │  │   CSS    │  │JavaScript│  │  AJAX    │           │   │
│  │  │(Rendered)│  │ (Styled) │  │ (jQuery) │  │(Dynamic) │           │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘           │   │
│  └───────┼─────────────┼─────────────┼─────────────┼──────────────────┘   │
└──────────┼─────────────┼─────────────┼─────────────┼───────────────────────┘
           │             │             │             │
           │ HTTP Request│             │ Event       │ XHR/Fetch
           │ GET/POST    │             │ Handlers    │ GET/POST
           │             │             │             │
┌──────────▼─────────────▼─────────────▼─────────────▼───────────────────────┐
│                         WEB SERVER TIER                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Gunicorn (WSGI Server)                                             │   │
│  │  - 4 workers (multiprocess)                                         │   │
│  │  - Handles HTTP requests                                            │   │
│  │  - Forwards to Django application                                   │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
└───────────────────────────────┼─────────────────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────────────────┐
│                       APPLICATION TIER (Django)                             │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  MIDDLEWARE STACK                                                    │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  1. SecurityMiddleware (HTTPS, headers)                      │  │   │
│  │  │  2. WhiteNoiseMiddleware (static files)                      │  │   │
│  │  │  3. SessionMiddleware (session management)                   │  │   │
│  │  │  4. CommonMiddleware (URL normalization)                     │  │   │
│  │  │  5. CsrfViewMiddleware (CSRF protection)                     │  │   │
│  │  │  6. AuthenticationMiddleware (user authentication)           │  │   │
│  │  │  7. MessageMiddleware (flash messages)                       │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                                │                                             │
│  ┌────────────────────────────▼────────────────────────────────────────┐   │
│  │  URL ROUTER (urls.py)                                               │   │
│  │  /              → core.views.index                                  │   │
│  │  /products/     → core.views.product_list                           │   │
│  │  /product/<id>/ → core.views.product_detail                         │   │
│  │  /cart/         → cartorders.views.cart_view                        │   │
│  │  /user/sign-in/ → userauths.views.login_view                        │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                                │                                             │
│  ┌────────────────────────────▼────────────────────────────────────────┐   │
│  │  VIEW LAYER (views.py)                                              │   │
│  │  1. Получить request                                                │   │
│  │  2. Аутентификация (если нужна)                                     │   │
│  │  3. Валидация данных (forms)                                        │   │
│  │  4. Бизнес-логика                                                   │   │
│  │  5. ORM запросы к БД                                                │   │
│  │  6. Подготовка context                                              │   │
│  │  7. Рендер template / JSON response                                 │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                                │                                             │
│  ┌────────────────────────────▼────────────────────────────────────────┐   │
│  │  ORM LAYER (Django ORM)                                             │   │
│  │  - QuerySet API                                                     │   │
│  │  - select_related() / prefetch_related()                            │   │
│  │  - Aggregation (Sum, Avg, Count)                                    │   │
│  │  - Transactions                                                     │   │
│  │  - Signals (post_save, pre_save)                                   │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
└───────────────────────────────┼─────────────────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────────────────┐
│                         DATABASE TIER                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  PostgreSQL (Supabase Cloud)                                        │   │
│  │  - 10+ tables (User, Product, Order, Review, etc.)                 │   │
│  │  - Indexes для оптимизации                                          │   │
│  │  - Foreign Keys constraints                                         │   │
│  │  - Connection pooling (pgBouncer)                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         CACHING TIER (Optional)                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Redis (Optional)                                                    │   │
│  │  - Session storage                                                   │   │
│  │  - Cache expensive queries                                           │   │
│  │  - Celery broker                                                     │   │
│  │  Fallback: LocMemCache (in-memory)                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                      BACKGROUND TASKS TIER                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Celery (Task Queue)                                                 │   │
│  │  - EAGER mode (synchronous, no worker needed)                       │   │
│  │  - Tasks: email notifications, order processing                     │   │
│  │  - Redis as broker                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         STATIC FILES TIER                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  WhiteNoise (Django Static Files Handler)                           │   │
│  │  - Serves CSS, JS, Images                                           │   │
│  │  - GZip compression                                                  │   │
│  │  - Cache headers                                                     │   │
│  │  - /static/ → /staticfiles/                                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         HOSTING TIER                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Render.com (Free Tier)                                              │   │
│  │  - Auto-deploy from GitHub                                           │   │
│  │  - HTTPS/SSL certificates                                            │   │
│  │  - Health checks                                                      │   │
│  │  - Environment variables                                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Workflow пользовательских сценариев

### 1. Регистрация пользователя (User Registration)

```
┌─────────┐                                                              
│ Browser │                                                              
└────┬────┘                                                              
     │ 1. GET /user/sign-up/                                            
     │────────────────────────────────────────►                         
     │                                        ┌──────────────┐          
     │                                        │ Django View  │          
     │                                        │ (register)   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 2. Render form    
     │                                               │    (UserRegisterForm)
     │                                               │                   
     │ 3. HTML form (empty)                          │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 4. User fills form & POST                                         
     │    (username, email, password)                                    
     │────────────────────────────────────────►                         
     │                                        ┌──────────────┐          
     │                                        │ Django View  │          
     │                                        │ (register)   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 5. Validate form  
     │                                               │    (UserRegisterForm)
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Check email  │          
     │                                        │ uniqueness   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 6. Create User    
     │                                               │    (save to DB)   
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ PostgreSQL   │          
     │                                        │ INSERT User  │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 7. post_save signal
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Create       │          
     │                                        │ Profile      │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 8. authenticate() 
     │                                               │    & login()      
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Create       │          
     │                                        │ Session      │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │ 9. Redirect to dashboard                      │                   
     │◄──────────────────────────────────────────────┘                   
     │    (Set session cookie)                                           
     │                                                                    
     │ 10. GET /dashboard/                                               
     │────────────────────────────────────────►                         
     │                                                                    
     │ 11. Dashboard page (logged in)                                    
     │◄────────────────────────────────────────                         
     │                                                                    
```

**Детальный процесс:**

1. **GET /user/sign-up/**
   - Middleware обрабатывает request
   - URL router направляет на `userauths.views.register_view`
   - View создает пустую форму `UserRegisterForm()`
   - Template рендерится с пустой формой
   - Response возвращается в браузер

2. **POST /user/sign-up/** (после заполнения)
   - CSRF middleware проверяет CSRF token
   - View получает `request.POST` данные
   - Создается `UserRegisterForm(request.POST)`
   - Валидация формы:
     * Username не пустой, уникальный
     * Email корректный формат, уникальный (case-insensitive)
     * Password соответствует требованиям
     * Password confirmation совпадает
   
3. **Создание пользователя**
   ```python
   new_user = form.save()  # User.objects.create_user(...)
   # Хеширование пароля (PBKDF2 SHA256)
   # INSERT INTO userauths_user (username, email, password, ...)
   ```

4. **Signal post_save**
   ```python
   @receiver(post_save, sender=User)
   def create_user_profile(sender, instance, created, **kwargs):
       if created:
           Profile.objects.create(user=instance)
   # INSERT INTO userauths_profile (user_id, ...)
   ```

5. **Автоматический вход**
   ```python
   user = authenticate(username=username, password=password)
   # EmailBackend проверяет credentials
   login(request, user)
   # Создание сессии в Redis/DB
   # Set-Cookie: sessionid=...
   ```

6. **Redirect**
   - HTTP 302 Redirect to `/dashboard/`
   - Browser следует redirect
   - Session cookie отправляется с новым запросом

---

### 2. Добавление товара в корзину (Add to Cart)

```
┌─────────┐                                                              
│ Browser │                                                              
└────┬────┘                                                              
     │ 1. Click "Add to Cart" button                                    
     │    (product_id=123)                                               
     │                                                                    
     │ 2. JavaScript event handler                                       
     │    triggered                                                      
     │                                                                    
     │ 3. AJAX POST /cart/add/123/                                       
     │    Headers: X-CSRFToken, X-Requested-With                         
     │────────────────────────────────────────►                         
     │                                        ┌──────────────┐          
     │                                        │ Django View  │          
     │                                        │ (add_to_cart)│          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 4. Check @login_required
     │                                               │    (session valid?)
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Get product  │          
     │                                        │ from DB      │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ SELECT * FROM product
     │                                               │ WHERE id=123      
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Get/Create   │          
     │                                        │ cart from    │          
     │                                        │ session      │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ cart = request.session.get('cart', {})
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Update cart  │          
     │                                        │ {123: {qty:1,│          
     │                                        │  price: 99}} │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ request.session['cart'] = cart
     │                                               │ request.session.modified = True
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Save session │          
     │                                        │ to Redis     │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │ 5. JSON response                              │                   
     │    {success: true, cart_count: 3}             │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 6. JavaScript updates UI                                          
     │    - Change button text to "Added"                                
     │    - Update cart badge count                                      
     │    - Show notification                                            
     │                                                                    
```

**Детальный процесс:**

1. **Client-side (JavaScript)**
   ```javascript
   $('.btn-add-to-cart').on('click', function(e) {
       e.preventDefault();
       const productId = $(this).data('product-id');
       
       $.ajax({
           url: `/cart/add/${productId}/`,
           method: 'POST',
           headers: {
               'X-CSRFToken': getCookie('csrftoken'),
               'X-Requested-With': 'XMLHttpRequest'
           },
           success: function(response) {
               updateCartCount();
               showNotification('Product added!', 'success');
           }
       });
   });
   ```

2. **Server-side (Django View)**
   ```python
   @login_required
   def add_to_cart(request, product_id):
       # 1. Получить товар
       product = get_object_or_404(Product, id=product_id, status="published")
       
       # 2. Получить корзину из сессии
       cart = request.session.get('cart', {})
       
       # 3. Добавить/обновить товар в корзине
       if str(product_id) in cart:
           cart[str(product_id)]['qty'] += 1
       else:
           cart[str(product_id)] = {
               'product_id': product.id,
               'title': product.title,
               'price': str(product.price),
               'qty': 1,
               'image': product.image.url if product.image else None,
               'vendor_id': product.vendor.id
           }
       
       # 4. Сохранить корзину в сессию
       request.session['cart'] = cart
       request.session.modified = True
       
       # 5. Вернуть JSON response
       return JsonResponse({
           'success': True,
           'cart_count': sum(item['qty'] for item in cart.values()),
           'message': f'{product.title} added to cart'
       })
   ```

3. **Session Storage (Redis)**
   ```
   Key: session:abc123def456
   Value: {
       '_auth_user_id': '42',
       'cart': {
           '123': {'qty': 1, 'price': '99.99', 'title': 'Laptop', ...},
           '456': {'qty': 2, 'price': '29.99', 'title': 'Mouse', ...}
       }
   }
   TTL: 86400 seconds (24 hours)
   ```

---

### 3. Оформление заказа (Checkout Process)

```
┌─────────┐                                                              
│ Browser │                                                              
└────┬────┘                                                              
     │ 1. User clicks "Checkout"                                         
     │    at /cart/                                                      
     │                                                                    
     │ 2. GET /checkout/                                                 
     │────────────────────────────────────────►                         
     │                                        ┌──────────────┐          
     │                                        │ Django View  │          
     │                                        │ (checkout)   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 3. Get cart from session
     │                                               │    Get user profile
     │                                               │    (address, etc.)
     │                                               │                   
     │ 4. Checkout form (pre-filled)                 │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 5. User reviews order                                             
     │    (can edit address, add notes)                                  
     │                                                                    
     │ 6. POST /checkout/                                                
     │    (confirm order)                                                
     │────────────────────────────────────────►                         
     │                                        ┌──────────────┐          
     │                                        │ Django View  │          
     │                                        │ (checkout)   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 7. Validate cart not empty
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ BEGIN        │          
     │                                        │ TRANSACTION  │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 8. Create CartOrder
     │                                               │    INSERT INTO cartorder
     │                                               │    (user_id, price, oid)
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ Loop through │          
     │                                        │ cart items   │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 9. For each item:
     │                                               │    INSERT INTO cartorderitem
     │                                               │    (order_id, product_id, qty, price)
     │                                               │                   
     │                                               ▼                   
     │                                        ┌──────────────┐          
     │                                        │ COMMIT       │          
     │                                        │ TRANSACTION  │          
     │                                        └──────┬───────┘          
     │                                               │                   
     │                                               │ 10. Clear cart from session
     │                                               │     request.session['cart'] = {}
     │                                               │                   
     │                                               │ 11. (Optional) Send email
     │                                               │     via Celery task
     │                                               │                   
     │ 12. Redirect to order detail                  │                   
     │     /order/<oid>/                             │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 13. GET /order/<oid>/                                             
     │────────────────────────────────────────►                         
     │                                                                    
     │ 14. Order detail page                                             
     │     (order #, items, total, status)                               
     │◄────────────────────────────────────────                         
     │                                                                    
```

**Database Transactions:**

```python
from django.db import transaction

@login_required
def checkout_view(request):
    if request.method == 'POST':
        cart = request.session.get('cart', {})
        
        if not cart:
            messages.error(request, "Your cart is empty!")
            return redirect('cartorders:cart')
        
        try:
            with transaction.atomic():
                # 1. Calculate total
                total_price = sum(
                    Decimal(item['price']) * item['qty'] 
                    for item in cart.values()
                )
                
                # 2. Create order
                order = CartOrder.objects.create(
                    user=request.user,
                    price=total_price,
                    paid_status=False,
                    oid=shortuuid.uuid()
                )
                
                # 3. Create order items
                for item_data in cart.values():
                    product = Product.objects.get(id=item_data['product_id'])
                    
                    CartOrderItem.objects.create(
                        order=order,
                        product=product,
                        vendor=product.vendor,
                        qty=item_data['qty'],
                        price=product.price,
                        total=product.price * item_data['qty']
                    )
                
                # Если что-то пойдет не так - все откатится (ROLLBACK)
            
            # 4. Clear cart (после успешной транзакции)
            request.session['cart'] = {}
            
            # 5. Send confirmation email (async via Celery)
            # send_order_confirmation_email.delay(order.id)
            
            messages.success(request, f"Order #{order.oid} created successfully!")
            return redirect('cartorders:order_detail', oid=order.oid)
            
        except Exception as e:
            logger.error(f"Checkout error: {e}")
            messages.error(request, "Failed to create order. Please try again.")
            return redirect('cartorders:cart')
```

---

## Request-Response циклы

### Синхронный запрос (SSR - Server-Side Rendering)

```
┌─────────┐                                                              
│ Browser │                                                              
└────┬────┘                                                              
     │                                                                    
     │ GET /products/?category=electronics&page=2                        
     │                                                                    
     ├──────────────────────────────────────►                           
     │                                        ┌──────────────────────┐  
     │                                        │ Render.com Server    │  
     │                                        │ (Reverse Proxy)      │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ Forward request   
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Gunicorn Worker      │  
     │                                        │ (WSGI)               │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ Call application  
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Django Middleware    │  
     │                                        │ Stack (7 layers)     │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ Process request   
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ URL Router           │  
     │                                        │ Match pattern        │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ /products/ → core.views.product_list
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ View Function        │  
     │                                        │ product_list_view()  │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 1. Parse GET params
     │                                               │    category='electronics'
     │                                               │    page=2
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Django ORM           │  
     │                                        │ Build query          │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 2. SELECT * FROM product
     │                                               │    WHERE category_id=5
     │                                               │    AND status='published'
     │                                               │    LIMIT 20 OFFSET 20
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ PostgreSQL           │  
     │                                        │ (Supabase)           │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 3. Return rows (20 products)
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ View Function        │  
     │                                        │ product_list_view()  │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 4. Prepare context
     │                                               │    {products: [...],
     │                                               │     page: 2, ...}
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Template Engine      │  
     │                                        │ product-list.html    │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 5. Render HTML
     │                                               │    Loop products
     │                                               │    Apply filters
     │                                               │    Include base.html
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Middleware Stack     │  
     │                                        │ (reverse order)      │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 6. Process response
     │                                               │    Add headers
     │                                               │    Compress (GZip)
     │                                               │                   
     │ 7. HTTP 200 OK                                │                   
     │    Content-Type: text/html                    │                   
     │    Content-Length: 15234                      │                   
     │    (HTML document)                            │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 8. Browser parses HTML                                            
     │    Renders page                                                   
     │    Loads CSS/JS from /static/                                     
     │                                                                    
```

**Timing breakdown:**
- Network latency: ~50-100ms
- Django processing: ~20-50ms
- Database query: ~10-30ms
- Template rendering: ~10-20ms
- **Total: ~90-200ms**

---

### Асинхронный запрос (AJAX)

```
┌─────────┐                                                              
│ Browser │                                                              
└────┬────┘                                                              
     │                                                                    
     │ Page already loaded                                               
     │ User types in search box                                          
     │                                                                    
     │ JavaScript: $('#search-input').on('keyup', ...)                   
     │                                                                    
     │ XHR/Fetch: GET /search/?q=laptop                                  
     │            X-Requested-With: XMLHttpRequest                       
     │                                                                    
     ├──────────────────────────────────────►                           
     │                                        ┌──────────────────────┐  
     │                                        │ Django View          │  
     │                                        │ search_view()        │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 1. Detect AJAX
     │                                               │    request.headers.get('X-Requested-With')
     │                                               │                   
     │                                               │ 2. Search query
     │                                               │    SELECT * FROM product
     │                                               │    WHERE title ILIKE '%laptop%'
     │                                               │    LIMIT 10
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ PostgreSQL           │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │                                               │ 3. Return results
     │                                               │                   
     │                                        ┌──────▼───────────────┐  
     │                                        │ Serialize to JSON    │  
     │                                        │ JsonResponse({...})  │  
     │                                        └──────┬───────────────┘  
     │                                               │                   
     │ 4. JSON response                              │                   
     │    {products: [                               │                   
     │      {id:1, title:'Laptop', price:999},       │                   
     │      {id:2, title:'Laptop Pro', price:1499}   │                   
     │    ]}                                          │                   
     │◄──────────────────────────────────────────────┘                   
     │                                                                    
     │ 5. JavaScript callback                                            
     │    success: function(data) {                                      
     │      displaySearchResults(data.products);                         
     │    }                                                               
     │                                                                    
     │ 6. DOM manipulation                                               
     │    $('#search-results').html(...)                                 
     │    Show dropdown with results                                     
     │                                                                    
```

**Timing breakdown:**
- Network latency: ~20-50ms
- Django processing: ~10-20ms
- Database query: ~5-15ms
- JSON serialization: ~5-10ms
- **Total: ~40-95ms** (Faster than SSR!)

---

## Data Flow диаграммы

### Product Display Flow

```
┌────────────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW: PRODUCT DISPLAY                         │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ PostgreSQL  │  1. Store raw data
│ Database    │     ┌─────────────────────────────────────────┐
└──────┬──────┘     │ product table:                          │
       │            │ - id: 123                               │
       │            │ - title: "Gaming Laptop"                │
       │            │ - price: 999.99                         │
       │            │ - category_id: 5 (FK)                   │
       │            │ - vendor_id: 10 (FK)                    │
       │            │ - image: "products/laptop.jpg"          │
       │            └─────────────────────────────────────────┘
       │
       ▼ 2. ORM Query
┌─────────────┐
│ Django ORM  │     Product.objects.select_related('category', 'vendor')\
└──────┬──────┘                    .prefetch_related('productimage_set',
       │                                             'review_set')\
       │                           .get(id=123)
       │
       ▼ 3. Python Object
┌─────────────┐
│ Product     │     product = <Product: Gaming Laptop>
│ Model       │     product.title = "Gaming Laptop"
│ Instance    │     product.price = Decimal('999.99')
└──────┬──────┘     product.category = <Category: Electronics>
       │            product.vendor = <Vendor: TechStore>
       │
       ▼ 4. Pass to template
┌─────────────┐
│ View        │     context = {
│ Function    │         'product': product,
└──────┬──────┘         'reviews': reviews,
       │                'related_products': related_products
       │            }
       │            return render(request, 'core/product-detail.html', context)
       │
       ▼ 5. Template rendering
┌─────────────┐
│ Template    │     {{ product.title }}            → "Gaming Laptop"
│ Engine      │     {{ product.price|floatformat:2 }} → "999.99"
└──────┬──────┘     {{ product.category.title }}  → "Electronics"
       │            {{ product.vendor.title }}     → "TechStore"
       │            {% for image in product.productimage_set.all %}
       │              <img src="{{ image.images.url }}">
       │            {% endfor %}
       │
       ▼ 6. HTML output
┌─────────────┐
│ HTML        │     <h1>Gaming Laptop</h1>
│ Response    │     <span class="price">$999.99</span>
└──────┬──────┘     <a href="/category/electronics/">Electronics</a>
       │            <p>Sold by: <a href="/vendor/10/">TechStore</a></p>
       │            <div class="gallery">
       │              <img src="/media/products/laptop.jpg">
       │              <img src="/media/products/laptop2.jpg">
       │            </div>
       │
       ▼ 7. Browser rendering
┌─────────────┐
│ Client      │     [Visual representation of product page]
│ Browser     │     - Product image
└─────────────┘     - Title, price, description
                    - Add to cart button
                    - Reviews section
```

---

### Order Processing Flow

```
┌────────────────────────────────────────────────────────────────────────────┐
│                       DATA FLOW: ORDER PROCESSING                          │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ Session     │  1. Cart stored in session (Redis)
│ Storage     │     cart = {
└──────┬──────┘         '123': {qty: 2, price: '999.99', ...},
       │                '456': {qty: 1, price: '29.99', ...}
       │            }
       │
       ▼ 2. User clicks "Checkout"
┌─────────────┐
│ View        │     checkout_view(request)
│ (checkout)  │     - Get cart from session
└──────┬──────┘     - Validate not empty
       │            - Calculate total
       │
       ▼ 3. BEGIN TRANSACTION
┌─────────────┐
│ Database    │     with transaction.atomic():
│ Transaction │         order = CartOrder.objects.create(...)
└──────┬──────┘         for item in cart:
       │                    CartOrderItem.objects.create(...)
       │
       ▼ 4. Create order record
┌─────────────────────────────────────────────────────────────────┐
│ cartorder table:                                                │
│ - id: 999                                                       │
│ - user_id: 42                                                   │
│ - price: 2029.97                                                │
│ - paid_status: False                                            │
│ - product_status: "processing"                                  │
│ - oid: "xyz789abc"  (shortuuid)                                │
│ - order_date: 2024-12-16 15:30:00                              │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼ 5. Create order items
┌─────────────────────────────────────────────────────────────────┐
│ cartorderitem table:                                            │
│ - id: 1001                                                      │
│ - order_id: 999 (FK)                                            │
│ - product_id: 123 (FK)                                          │
│ - vendor_id: 10 (FK)                                            │
│ - qty: 2                                                        │
│ - price: 999.99                                                 │
│ - total: 1999.98                                                │
│                                                                  │
│ - id: 1002                                                      │
│ - order_id: 999 (FK)                                            │
│ - product_id: 456 (FK)                                          │
│ - vendor_id: 10 (FK)                                            │
│ - qty: 1                                                        │
│ - price: 29.99                                                  │
│ - total: 29.99                                                  │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼ 6. COMMIT TRANSACTION
┌─────────────┐
│ Post-order  │     - Clear cart from session
│ Processing  │     - Send confirmation email (Celery)
└──────┬──────┘     - Redirect to order detail page
       │
       ▼ 7. (Optional) Send email
┌─────────────┐
│ Celery Task │     send_order_confirmation_email.delay(order.id)
│ (async)     │     - Get order from DB
└──────┬──────┘     - Format email body
       │            - Call send_mail()
       │
       ▼ 8. Email delivery
┌─────────────┐
│ SMTP Server │     Email sent to user@example.com
│ (Console/   │     Subject: "Order Confirmation #xyz789abc"
│  Gmail)     │     Body: Order details, items, total
└─────────────┘
```

---

## Deployment Pipeline

### CI/CD Workflow (GitHub → Render.com)

```
┌────────────────────────────────────────────────────────────────────────────┐
│                       DEPLOYMENT PIPELINE                                   │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ Developer   │  1. Write code locally
│ (Local PC)  │     - Edit views.py, models.py, templates
└──────┬──────┘     - Test locally (python manage.py runserver)
       │            - Git add, commit
       │
       │ 2. git push origin daniyar
       │
       ▼
┌─────────────┐
│ GitHub      │  3. Code pushed to repository
│ Repository  │     Repo: Daniyar008/ecom_prj
└──────┬──────┘     Branch: daniyar
       │
       │ 4. Webhook triggered
       │
       ▼
┌─────────────┐
│ Render.com  │  5. Auto-deploy initiated
│ Platform    │     - Detect push to branch
└──────┬──────┘     - Pull latest code
       │            - Start build process
       │
       ▼ 6. Build process
┌────────────────────────────────────────────────────────────────┐
│ build.sh:                                                      │
│                                                                 │
│ #!/usr/bin/env bash                                            │
│ set -o errexit                                                 │
│                                                                 │
│ # Install dependencies                                         │
│ pip install -r requirements.txt                                │
│                                                                 │
│ # Collect static files                                         │
│ python manage.py collectstatic --no-input --clear             │
│                                                                 │
│ # Run migrations                                               │
│ python manage.py migrate --noinput                             │
│                                                                 │
│ echo "✅ Build completed successfully!"                        │
└────────────────────────────────────────────────────────────────┘
       │
       ▼ 7. Install dependencies (2-3 minutes)
┌─────────────┐
│ pip install │     Installing:
│ -r          │     - Django==4.2.7
│ requirements│     - psycopg2-binary==2.9.9
│ .txt        │     - gunicorn==21.2.0
└──────┬──────┘     - redis, celery, pillow, etc.
       │            Total: 50+ packages
       │
       ▼ 8. Collect static files (~30 seconds)
┌─────────────┐
│ collectstatic│    Copying:
│             │     - assets/css/*.css → staticfiles/
└──────┬──────┘     - assets/js/*.js → staticfiles/
       │            - assets/imgs/* → staticfiles/
       │            Total: 718 static files
       │
       ▼ 9. Run database migrations (~10 seconds)
┌─────────────┐
│ migrate     │     Applying migrations:
│             │     - userauths: 0001_initial, 0002_user_bio, ...
└──────┬──────┘     - core: 0001_initial, ...
       │            - cartorders: 0001_initial, ...
       │            Total: 45 migrations
       │
       ▼ 10. Start Gunicorn server
┌────────────────────────────────────────────────────────────────┐
│ Gunicorn configuration:                                        │
│                                                                 │
│ gunicorn ecomprj.wsgi:application                              │
│   --bind 0.0.0.0:$PORT                                         │
│   --workers 4                                                   │
│   --timeout 120                                                 │
│   --access-logfile -                                            │
│   --error-logfile -                                             │
│                                                                 │
│ Worker processes: 4 (multiprocessing)                          │
│ Port: 10000 (internal)                                         │
│ Timeout: 120 seconds per request                               │
└────────────────────────────────────────────────────────────────┘
       │
       ▼ 11. Health check
┌─────────────┐
│ Render.com  │     GET /health/ (if configured)
│ Health      │     or GET / (default)
│ Check       │     Expected: HTTP 200
└──────┬──────┘     Retry: 3 times, 30s interval
       │
       ▼ 12. Deployment complete
┌─────────────┐
│ Live Site   │     ✅ Deployment successful!
│             │     🌐 https://multivendor-shop-iz5t.onrender.com
└─────────────┘     📅 Deployed at: 2024-12-16 15:45:32
                    ⏱️  Build time: ~3-4 minutes
                    🚀 Auto-deploy from: daniyar branch
```

**Environment Variables (Render.com dashboard):**
```
DATABASE_URL=postgresql://postgres.qfchxuejdcwjvwfpximq:...
SECRET_KEY=django-insecure-*******************
DEBUG=False
ALLOWED_HOSTS=multivendor-shop-iz5t.onrender.com
REDIS_URL=redis://red-d4vpuf24d50c73865p10:6379
CELERY_BROKER_URL=redis://red-d4vpuf24d50c73865p10:6379/0
CELERY_TASK_ALWAYS_EAGER=True
CELERY_TASK_EAGER_PROPAGATES=False
```

---

## Monitoring & Logging

### Logging Flow

```
┌────────────────────────────────────────────────────────────────────────────┐
│                           LOGGING ARCHITECTURE                              │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ Application │  Code generates log messages
│ Code        │  
└──────┬──────┘  logger.info("User registered: john@example.com")
       │         logger.error("Database connection failed", exc_info=True)
       │         logger.debug("Cart updated: 3 items")
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│ Python Logging Module                                          │
│                                                                  │
│ import logging                                                  │
│ logger = logging.getLogger(__name__)                           │
│                                                                  │
│ Loggers по уровням:                                             │
│ - DEBUG (10): Детальная информация для debugging               │
│ - INFO (20): Подтверждение что все работает                    │
│ - WARNING (30): Предупреждения (неожиданные события)           │
│ - ERROR (40): Ошибки (функционал не работает)                  │
│ - CRITICAL (50): Критические ошибки (приложение падает)        │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼ Filter by level & format
┌─────────────────────────────────────────────────────────────────┐
│ Formatters                                                       │
│                                                                  │
│ 'verbose': '{levelname} {asctime} {module} {message}'          │
│                                                                  │
│ Output example:                                                 │
│ INFO 2024-12-16 15:30:45 views User registered: john@example.com│
└──────┬──────────────────────────────────────────────────────────┘
       │
       ├─────────────────┬─────────────────┐
       │                 │                 │
       ▼                 ▼                 ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Console  │    │ File     │    │ External │
│ Handler  │    │ Handler  │    │ Service  │
└────┬─────┘    └────┬─────┘    └────┬─────┘
     │               │               │
     │ stdout        │ django.log    │ Sentry.io
     │               │ /app/logs/    │ (optional)
     │               │               │
     ▼               ▼               ▼
[Terminal]    [Log File]      [Cloud Dashboard]
```

**Log Output Examples:**

```
# Console (stdout) - visible in Render.com logs
INFO 2024-12-16 15:30:45 userauths.views User registered: john@example.com
INFO 2024-12-16 15:30:46 userauths.models Profile created for user john
INFO 2024-12-16 15:30:47 django.request "GET /dashboard/ HTTP/1.1" 200 5432

# File (django.log) - stored in /app/logs/
ERROR 2024-12-16 15:32:10 cartorders.views Checkout error: Insufficient stock
Traceback (most recent call last):
  File "/app/cartorders/views.py", line 45, in checkout_view
    order = create_order(request, cart)
  File "/app/cartorders/utils.py", line 23, in create_order
    raise InsufficientStock("Product not available")
InsufficientStock: Product not available

# Render.com Logs Dashboard (real-time)
[INFO] Starting gunicorn 21.2.0
[INFO] Listening at: http://0.0.0.0:10000
[INFO] Using worker: sync
[INFO] Booting worker with pid: 42
[INFO] Booting worker with pid: 43
[INFO] Booting worker with pid: 44
[INFO] Booting worker with pid: 45
```

---

## Performance Metrics

### Response Time Breakdown

```
┌────────────────────────────────────────────────────────────────────────────┐
│                     AVERAGE REQUEST TIMING (ms)                             │
└────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 1. DNS Lookup: ~5-20ms                                  │
│    multivendor-shop-iz5t.onrender.com → IP address     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 2. TCP Handshake: ~10-30ms                              │
│    SYN → SYN-ACK → ACK                                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 3. TLS Handshake (HTTPS): ~20-50ms                      │
│    Certificate exchange, encryption setup               │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 4. HTTP Request: ~5-10ms                                │
│    Send request headers and body                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 5. Server Processing: ~50-200ms                         │
│    ├─ Middleware: ~5-10ms                               │
│    ├─ View logic: ~10-20ms                              │
│    ├─ Database query: ~10-50ms                          │
│    │   └─ With select_related: ~10-20ms                 │
│    │   └─ Without optimization: ~50-100ms               │
│    ├─ Cache lookup (if hit): ~1-5ms                     │
│    └─ Template rendering: ~20-50ms                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 6. HTTP Response: ~5-20ms                               │
│    Send response headers and body                       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 7. Browser Rendering: ~100-500ms                        │
│    Parse HTML, load CSS/JS, render DOM                  │
└─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════
TOTAL: ~195-830ms (First Load)
TOTAL: ~50-200ms (With Cache & Optimizations)
═══════════════════════════════════════════════════════════
```

---

## Заключение Integration & Workflow

✅ **Full Stack Integration:**
- Client (Browser) ↔ Server (Django) ↔ Database (PostgreSQL)
- AJAX для динамического контента
- SSR для основных страниц
- Session management через Redis

✅ **User Workflows документированы:**
- Registration (8 шагов)
- Add to Cart (6 шагов)
- Checkout (14 шагов с транзакцией)

✅ **Request-Response циклы:**
- Синхронный (SSR): ~90-200ms
- Асинхронный (AJAX): ~40-95ms

✅ **Deployment Pipeline:**
- GitHub → Render.com auto-deploy
- Build time: ~3-4 минуты
- 45 migrations, 718 static files

✅ **Monitoring:**
- Comprehensive logging (Console + File)
- Performance metrics documented
- Error tracking with exc_info

Следующие документы:
- USE_CASE_DIAGRAMS.md
- PROJECT_REPORT.md
