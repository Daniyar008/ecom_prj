# Настройка Redis на Render.com

## Что такое Redis?
Redis - это быстрое хранилище данных в памяти, которое используется для кеширования. Это значительно ускоряет загрузку страниц, так как данные берутся из Redis (очень быстро), а не из базы данных PostgreSQL (медленнее).

## Преимущества кеширования с Redis:
✅ **Быстрая загрузка страниц** - данные в памяти загружаются в 100+ раз быстрее
✅ **Меньше нагрузки на БД** - снижает количество запросов к Supabase
✅ **Лучший UX** - пользователи видят контент мгновенно
✅ **Кеширование сессий** - быстрая авторизация пользователей

## Создание Redis на Render.com

### Шаг 1: Создайте Redis instance
1. Зайдите в Render Dashboard
2. Нажмите **"New +"** → **"Redis"**
3. Заполните форму:
   - **Name**: `ecomprj-redis`
   - **Region**: тот же регион что и ваш Web Service (для минимальной задержки)
   - **Plan**: **Free** (25 MB, достаточно для старта)
   - **Maxmemory Policy**: `allkeys-lru` (удаляет старые ключи когда память заполнена)
4. Нажмите **"Create Redis"**
5. Подождите 1-2 минуты пока Redis создастся

### Шаг 2: Получите Redis URL
1. После создания откройте Redis instance
2. В разделе **"Info"** найдите **"Internal Redis URL"**
3. Скопируйте URL, он выглядит примерно так:
   ```
   redis://red-xxxxxxxxxxxxx:6379
   ```

### Шаг 3: Добавьте Redis URL в переменные окружения
1. Откройте ваш Web Service в Render
2. Перейдите в **Environment**
3. Добавьте новую переменную:
   - **Key**: `REDIS_URL`
   - **Value**: вставьте Internal Redis URL из шага 2
4. Нажмите **"Save Changes"**

### Шаг 4: Деплой
После добавления `REDIS_URL` Render автоматически перезапустит сервис с новыми настройками.

## Проверка работы Redis

### Через Render Dashboard:
1. Откройте ваш Redis instance
2. Во вкладке **"Metrics"** вы увидите статистику использования

### Через Django Shell:
```python
python manage.py shell

from django.core.cache import cache

# Установить значение
cache.set('test_key', 'Hello Redis!', 60)

# Получить значение
value = cache.get('test_key')
print(value)  # Должно вывести: Hello Redis!
```

## Что кешируется в проекте?

### 1. Целые страницы (5 минут)
- Главная страница
- Списки товаров
- Категории
- Публичные страницы

### 2. Сессии пользователей
- Данные авторизации
- Корзина покупок
- Состояние сессии

### 3. База данных queries
Django автоматически кеширует результаты запросов к БД

## Очистка кеша

### Вручную через Django shell:
```python
from django.core.cache import cache
cache.clear()
```

### Автоматически:
Кеш очищается автоматически через 5 минут (настройка `CACHE_MIDDLEWARE_SECONDS`)

## Отключение кеша для разработки

Если нужно отключить Redis локально, закомментируйте в `.env`:
```env
# REDIS_URL=redis://127.0.0.1:6379/1
```

При отсутствии `REDIS_URL` Django будет использовать fallback на локальную память.

## Мониторинг

В Render Dashboard → Redis → Metrics вы можете видеть:
- Используемую память
- Количество ключей
- Hit/Miss ratio (эффективность кеша)
- Количество подключений

## Важные замечания

⚠️ **Free план Redis на Render:**
- 25 MB памяти
- Достаточно для ~50,000 кешированных ключей
- Автоматически удаляет старые данные при заполнении

⚠️ **После деплоя:**
- Первые запросы будут медленнее (заполнение кеша)
- Последующие запросы будут очень быстрыми
- Кеш сбрасывается при перезапуске сервиса

## Готово! 🚀

После настройки ваш сайт будет загружаться намного быстрее!
