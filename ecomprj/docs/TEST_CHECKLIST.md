# ✅ Чек-лист тестирования после деплоя

## 🔥 КРИТИЧЕСКИЕ ФУНКЦИИ

### 1. Регистрация
- [ ] Открыть `/user/sign-up/`
- [ ] Заполнить форму (email, username, password)
- [ ] Нажать "Register"
- [ ] ✅ **ОЖИДАЕТСЯ**: Автоматический вход → перенаправление на dashboard
- [ ] ❌ **БЫЛО**: "Registration failed: HiredisParser..."

### 2. Вход в систему
- [ ] Открыть `/user/sign-in/`
- [ ] Ввести email (или username) + пароль
- [ ] Нажать "Login"
- [ ] ✅ **ОЖИДАЕТСЯ**: Успешный вход → dashboard
- [ ] ❌ **БЫЛО**: "Login error: HiredisParser..."

### 3. Админ-панель
- [ ] Открыть `/admin/`
- [ ] Войти под суперпользователем
- [ ] ✅ **ОЖИДАЕТСЯ**: Доступ к Django Admin
- [ ] Проверить список пользователей (Users)

### 4. Проверка сессий (Redis)
- [ ] Войти в систему
- [ ] Обновить страницу несколько раз
- [ ] ✅ **ОЖИДАЕТСЯ**: Сессия сохраняется, не выбрасывает
- [ ] Закрыть браузер, открыть снова
- [ ] ✅ **ОЖИДАЕТСЯ**: Все еще залогинен (если не прошло 2 недели)

---

## 📊 ЧТО СЕЙЧАС РАБОТАЕТ (установлено)

### ✅ Инфраструктура
- **Hosting**: Render.com (Free tier)
- **Database**: Supabase PostgreSQL
- **Cache**: Redis (optional, с fallback на LocMemCache)
- **Static files**: WhiteNoise
- **Web server**: Gunicorn

### ✅ Django конфигурация
- **Debug**: OFF (production)
- **ALLOWED_HOSTS**: настроены
- **CSRF/CORS**: настроены
- **Static/Media**: собираются и раздаются

### ✅ Авторизация
- **Custom EmailBackend**: вход по email ИЛИ username
- **Case-insensitive email**: TEST@mail.com = test@mail.com
- **Password validation**: стандартная Django
- **Sessions**: хранятся в Redis (или memory fallback)

### ✅ Celery (бесплатная конфигурация)
- **Режим**: EAGER (синхронный, без воркера)
- **Broker**: Redis
- **Tasks**: email уведомления, заказы, отчеты
- **Безопасность**: не падает если Redis недоступен

### ✅ Email
- **Backend**: Console (выводит в Render logs)
- **Готово к настройке**: SMTP (Gmail/SendGrid/Mailgun)

### ✅ Логирование
- **Уровень**: DEBUG для userauths/core/cartorders
- **Вывод**: Render Console (доступно в Dashboard)

---

## 🚀 ЧТО МОЖНО ДОБАВИТЬ (опционально)

### 🔒 Безопасность (РЕКОМЕНДУЕТСЯ)

#### 1. HTTPS принудительно
```python
# ecomprj/settings.py
SECURE_SSL_REDIRECT = True
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

#### 2. Rate limiting (защита от спама)
```bash
pip install django-ratelimit
```

#### 3. Security headers
```bash
pip install django-csp
```

---

### 📧 Email настройка (ДЛЯ ПРОДАКШНА)

#### Gmail SMTP:
```env
# В Render Environment Variables добавить:
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password  # НЕ обычный пароль!
DEFAULT_FROM_EMAIL=your-email@gmail.com
```

**Как получить App Password:**
1. Google Account → Security → 2-Step Verification → App passwords
2. Создать новый → "Django App" → скопировать 16-значный код

#### SendGrid (лучше для продакшна):
```bash
pip install sendgrid
```

---

### ⚡ Производительность

#### 1. Асинхронный Celery (если нужно)
**Если** Email/задачи работают медленно, переключить на async:

- Deploy Celery Worker на Railway.app (500 часов/мес бесплатно)
- Изменить `CELERY_TASK_ALWAYS_EAGER = False`
- Инструкция в `CELERY_FREE_OPTIONS.md`

#### 2. CDN для static файлов (если медленно)
```bash
# AWS CloudFront или Cloudflare
pip install django-storages[boto3]
```

#### 3. Database connection pooling
```bash
pip install psycopg2-pool
```

---

### 🎨 Функциональность

#### 1. Восстановление пароля
- Email с ссылкой для сброса
- Требует настройки SMTP

#### 2. Email подтверждение
```python
# При регистрации отправлять verification email
EMAIL_VERIFY_REQUIRED = True
```

#### 3. Social Auth (Google/Facebook)
```bash
pip install social-auth-app-django
```

#### 4. Двухфакторная аутентификация (2FA)
```bash
pip install django-otp
```

#### 5. Мониторинг ошибок
```bash
# Sentry.io (бесплатно для 5000 events/мес)
pip install sentry-sdk
```

---

### 📊 Аналитика и мониторинг

#### 1. Uptime monitoring
- UptimeRobot.com (бесплатно, проверяет каждые 5 мин)
- Better Uptime

#### 2. Логи и метрики
- Papertrail (бесплатно 50 MB/мес)
- Logtail

#### 3. Performance monitoring
- New Relic (бесплатно 100 GB/мес)

---

### 💳 Платежи (если нужно доработать)

**Сейчас подключено:**
- Stripe ✅
- PayPal ✅

**Можно добавить:**
- YooKassa (для России)
- Robokassa
- Cryptocurrency payments

---

## 📝 ТЕКУЩИЙ СТАТУС ПРОЕКТА

### Что РАБОТАЕТ прямо сейчас:
```
✅ Сайт доступен 24/7
✅ База данных (Supabase)
✅ Регистрация/Вход (исправлено)
✅ Админ-панель
✅ Static файлы
✅ Redis кеширование (опционально)
✅ Celery в EAGER режиме
✅ Логирование всех ошибок
```

### Что НУЖНО для продакшна:
```
🔴 ОБЯЗАТЕЛЬНО:
   - Настроить SMTP email (сейчас только console)
   - Создать суперпользователя (если еще нет)
   
🟡 ЖЕЛАТЕЛЬНО:
   - Включить HTTPS принудительно
   - Настроить rate limiting
   - Настроить мониторинг uptime
   
🟢 ОПЦИОНАЛЬНО:
   - Async Celery (если медленно)
   - CDN (если трафик большой)
   - Social auth
```

---

## 🔧 КАК ПРОВЕРИТЬ ПРЯМО СЕЙЧАС

### 1. Дождаться завершения деплоя (2-3 мин)
Проверить статус: https://dashboard.render.com

### 2. Открыть сайт
https://multivendor-shop-iz5t.onrender.com/

### 3. Попробовать зарегистрироваться
- Новый email
- Username
- Пароль (минимум 8 символов)

### 4. Если успешно → ВСЁ РАБОТАЕТ! 🎉

### 5. Если ошибка → Смотрим логи:
```
Render Dashboard → Your Service → Logs
Искать строки с [ERROR] или "Registration failed"
```

---

## 📞 Получить помощь

Если что-то не работает после деплоя:

1. **Скопировать логи** из Render (последние 20-30 строк с ошибкой)
2. **Показать мне** - я помогу исправить
3. **Проверить** DEBUG_500_ERROR.md - там инструкции по диагностике

---

## 🎯 ПРИОРИТЕТЫ НА БЛИЖАЙШЕЕ ВРЕМЯ

### Прямо сейчас (после успешного теста):
1. ✅ Убедиться что регистрация/вход работают
2. ✅ Создать суперпользователя (если нет)
3. ✅ Проверить админку

### На этой неделе:
1. 📧 Настроить реальный SMTP email
2. 🔒 Включить HTTPS принудительно
3. 📊 Настроить uptime monitoring

### Когда будет время:
1. ⚡ Переключить Celery на async (если нужна скорость)
2. 🎨 Добавить восстановление пароля
3. 📈 Настроить аналитику

---

## ❓ FAQ

**Q: Нужно ли мне что-то менять в коде сейчас?**
A: НЕТ. Все уже настроено и работает. Просто протестируй после деплоя.

**Q: Как узнать что деплой завершился?**
A: Render Dashboard → Your Service → последняя запись "Deploy succeeded" (зеленая галочка)

**Q: Сколько стоит текущая инфраструктура?**
A: **$0/месяц** - всё на бесплатных тирах (Render + Supabase + Redis)

**Q: Когда нужно платить?**
A: Только если:
- Трафик > 100GB/мес (Render)
- База > 500MB (Supabase)
- Redis > 30MB (Render)
- Нужен async Celery worker 24/7

**Q: Безопасно ли для продакшна?**
A: ДА, НО:
- ✅ Код безопасный
- ✅ HTTPS работает
- 🟡 Настрой rate limiting (защита от спама)
- 🟡 Настрой real SMTP (сейчас console)
